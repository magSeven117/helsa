{
  "name": "@helsa/rate-limit",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "clean": "git clean -xdf .cache .turbo dist node_modules",
    "typecheck": "tsc --noEmit --emitDeclarationOnly false"
  },
  "dependencies": {
    "@upstash/ratelimit": "^2.0.4",
    "@upstash/redis": "^1.34.3"
  },
  "devDependencies": {
    "@helsa/typescript-config": "*",
    "typescript": "^5"
  }
}
