scalar DateTime
type Query {
  hello: String
  date: DateTime
}

# User types

enum Role {
  DOCTOR
  PATIENT
}

type User {
  id: ID!
  externalId: String!
  email: String!
  role: String!
  createdAt: DateTime!
  updatedAt: DateTime!
}